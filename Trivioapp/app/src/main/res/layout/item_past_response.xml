<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/pink"
    android:layout_marginBottom="8dp">

    <!-- Question Text -->
    <TextView
        android:id="@+id/tv_question"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/questionPast"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:contentDescription="@string/display_the_question_text" />

    <!-- User's Answer -->
    <TextView
        android:id="@+id/tv_user_answer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/your_answer"
        android:layout_marginTop="4dp"
        android:textColor="@color/black"
        android:textSize="14sp"
        android:contentDescription="@string/display_the_user_s_answer" />

    <!-- Correct Answer Indicator -->
    <TextView
        android:id="@+id/tv_correct_answer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/correct_answer"
        android:layout_marginTop="4dp"
        android:textColor="@color/black"
        android:textSize="14sp"
        android:contentDescription="@string/display_the_correct_answer" />

    <!-- Timestamp -->
    <TextView
        android:id="@+id/tv_response_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/date"
        android:layout_marginTop="4dp"
        android:textColor="@color/black"
        android:textSize="12sp"
        android:contentDescription="@string/display_the_timestamp_for_the_response" />
</LinearLayout>
